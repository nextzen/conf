checkout:
  post:
    - git submodule sync
    - git submodule update --init

dependencies:
  override:
    - sudo apt-get install jq

test:
  override:
    - jq '.' valhalla.json

general:
  artifacts:
    - valhalla.json
