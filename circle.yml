checkout:
  post:
    - git submodule sync
    - git submodule update --init

dependencies:
  override:
    - sudo apt-get install jq

test:
  override:
    - jq '.' valhalla.json
    - ls this_is_not_a_file

general:
  artifacts:
    - valhalla.json
